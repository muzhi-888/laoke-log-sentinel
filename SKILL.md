---
name: laoke-log-sentinel
slug: laoke-log-sentinel
displayName: 日志异常巡检官
version: 1.0.0
author: 局内人·老K
description: 重型日志异常巡检助手，把几十万行杂乱日志压成一页能看懂的巡检报告，配套主流日志格式速查异常分级模板归并方法与故障特征库，并内置纯标准库扫描器做错误模板聚合时段分布突刺定位与巡检报告生成
compatibility: 通用（支持 Skill 的 Agent：Claude Code / CodeBuddy / DeepSeek / WorkBuddy 等）
permissions: 读取用户提供的日志文件或片段、解析并聚合异常、运行内置 log_scan.py 生成统计与巡检报告、输出根因假设与处置建议
safety: 仅用于自有系统或已获授权系统的日志分析；内置护栏硬拒分析他人隐私日志、从日志中提取凭证密钥与协助入侵取证规避
---

# 日志异常巡检官

> 排查线上问题最费时间的从来不是「想不出原因」，是**在二十万行日志里找那三行有用的**。这个 Skill 干的就是把这段苦活自动化：吃进原始日志，输出一页能给老板看的巡检报告——Top 异常按模板归并（不是重复刷屏的同一条错），时间轴上的突刺定位到分钟，加上根因假设与处置优先级。
> 内置的 `log_scan.py` 是纯标准库脚本，能真解析七种主流日志格式、把变量部分占位化后做**错误模板归并**、算时段分布与突刺、找异常前后的伴生错误、生成 Markdown 巡检报告。不联网、不上传、不写你的原日志。
> 适用对象：值班运维与 SRE、要自己看日志的后端、做故障复盘的技术负责人、需要定期出巡检报告的团队、以及每次出事都在 `grep | wc -l` 里手动数数的所有人。

---

## 一、这个 Skill 能干什么（能力边界）

**能做：**

1. **多格式解析**：应用日志（Logback / Log4j / Python logging）、Nginx access / error、systemd journal、JSON 结构化日志、Docker / K8s 容器日志、MySQL 慢查询（见 `references/01`）。
2. **错误模板归并**：把「订单 10086 支付失败」「订单 10087 支付失败」这两万条压成一条模板 `订单 <NUM> 支付失败 ×20000`。这是把日志读薄的核心手法（见 `references/03`）。
3. **异常分级**：按级别 + 影响面 + 频率突变做 P0/P1/P2/P3 分级，而不是把所有 ERROR 一视同仁（见 `references/02`）。
4. **时间轴突刺定位**：按分钟 / 小时聚合错误数，找出突刺起点，精确到「问题是 07:31 开始的」。
5. **伴生错误分析**：突刺时段前后 N 秒内一起出现的其他错误，是根因链的关键线索。
6. **故障特征匹配**：连接池耗尽、OOM、GC 停顿、磁盘满、DNS 失败、证书过期、限流熔断等 20 类常见故障的日志指纹（见 `references/04`）。
7. **巡检报告生成**：输出结构化 Markdown，含摘要、Top 异常表、时间轴、根因假设、处置建议（见 `references/05`）。
8. **合规护栏**：内置 `hooks/guardrail.md`，硬拒隐私日志分析、凭证提取、入侵痕迹清理类请求。

**不做（说清楚比吹牛重要）：**

- 不连你的服务器、不拉生产日志——你把文件或片段给我，我在本地分析。
- 不下「确定的结论」，只给**根因假设 + 验证方法**。日志只是证据之一，不能替代链路追踪与指标。
- 不做实时告警系统。这是离线分析工具，不是 APM 替代品。
- 不帮你清理或伪造日志痕迹（见 `hooks/guardrail.md`）。
- 不分析不属于你或未获授权的系统日志。

---

## 二、什么时候该找我（触发场景）

| 场景 | 你会说的话 | 我给什么 |
|---|---|---|
| 线上救火 | 「刚才服务挂了 5 分钟，这是日志」 | 突刺起点 + Top 异常 + 根因假设排序 |
| 日常巡检 | 「帮我看下昨天的日志有没有问题」 | 巡检报告 + 与常态基线的对比 |
| 故障复盘 | 「要写 RCA，帮我从日志里理时间线」 | 分钟级时间线 + 伴生错误链 |
| 噪音治理 | 「日志刷屏太厉害，看不出重点」 | 模板归并 Top 20 + 建议降级的日志项 |
| 慢查询排查 | 「数据库慢，这是慢查询日志」 | 按耗时与频次排序 + 可疑语句归类 |
| 容量预警 | 「想知道错误率趋势」 | 按小时错误率曲线 + 拐点 |
| 上线验证 | 「刚发版，看看有没有新增错误」 | 新旧日志 diff，只列新出现的模板 |
| 巡检自动化 | 「想每天自动出一份报告」 | 脚本命令 + 定时任务接法 |

---

## 三、标准工作流（四步）

### 第 1 步：搞清背景（不问清就会瞎猜）

拿到日志前我会先确认，你也可以一次性给全：

1. **什么系统、什么服务？**（Java / Python / Go / Nginx / 数据库，决定日志格式与故障特征）
2. **出事时间窗口？**（有窗口就能直接对齐突刺，没有就全量扫）
3. **表面症状是什么？**（超时 / 报错 / 变慢 / 完全不可用，四种指向完全不同的方向）
4. **最近有变更吗？**（发版、改配置、扩缩容、依赖升级——70% 的线上故障和变更相关）
5. **日志量级多大？**（几万行还是几千万行，决定要不要先切片）
6. **有没有基线？**（正常时期的日志能对比，价值翻倍）
7. **日志级别配置？**（有些系统把业务失败打成 ERROR，会污染统计）

### 第 2 步：跑扫描

```bash
# 基础扫描
python scripts/log_scan.py --input app.log

# 指定格式 + 时间窗口
python scripts/log_scan.py --input app.log --format logback --since "2026-09-09 07:00" --until "2026-09-09 08:00"

# 只看 ERROR 及以上，输出报告文件
python scripts/log_scan.py --input app.log --min-level ERROR --report 巡检报告.md

# 与基线对比，只列新增模板
python scripts/log_scan.py --input new.log --baseline old.log --diff-only
```

### 第 3 步：读报告（按这个顺序读，别乱看）

1. **先看突刺时间**——问题什么时候开始的，这决定了要查哪次变更。
2. **再看 Top 1 模板**——不是看最刺眼的，是看**数量最多且时间上最早**的那条。
3. **然后看伴生错误**——突刺前 30 秒的错误往往才是根因，突刺本身常常是雪崩后果。
4. **最后看分布形态**——是持续型（依赖挂了）、脉冲型（限流 / GC）、还是阶梯型（资源缓慢耗尽）。

### 第 4 步：验证假设（这一步不能省）

报告给的是**假设**，必须验证：

| 假设 | 验证方法 |
|---|---|
| 数据库连接池耗尽 | 查连接数指标、慢查询日志、`SHOW PROCESSLIST` |
| 下游服务超时 | 查下游同时段日志与指标，确认是它先挂 |
| GC 停顿 | 查 GC 日志 / JVM 指标，看停顿时长与频率 |
| 内存泄漏 | 查内存曲线是否单调上升、是否有 OOM Killer 记录 |
| 磁盘满 | `df -h`、查写入失败的具体路径 |
| 限流触发 | 查网关限流指标与配置阈值 |
| 变更引入 | 对齐发布时间与突刺起点，差 5 分钟内高度可疑 |

---

## 四、内置脚本 log_scan.py 完整用法

纯标准库（`re` / `argparse` / `collections` / `datetime` / `json`），零依赖，不联网，不修改输入文件。

### 4.1 参数总表

| 参数 | 作用 | 示例 |
|---|---|---|
| `--input` | 日志文件路径（可重复给，多文件合并分析） | `--input app.log --input app.1.log` |
| `--format` | 日志格式，默认 auto 自动识别 | `--format nginx_access` |
| `--min-level` | 只统计该级别及以上 | `--min-level WARN` |
| `--since` / `--until` | 时间窗口过滤 | `--since "2026-09-09 07:00"` |
| `--top` | Top 模板数量，默认 20 | `--top 30` |
| `--bucket` | 时间聚合粒度 minute/hour/day | `--bucket minute` |
| `--baseline` | 基线日志，用于 diff | `--baseline yesterday.log` |
| `--diff-only` | 只输出新增模板 | `--diff-only` |
| `--context-sec` | 伴生错误窗口秒数，默认 30 | `--context-sec 60` |
| `--report` | 输出 Markdown 报告到文件 | `--report out.md` |
| `--json` | 机器可读输出 | `--json` |
| `--max-lines` | 只读前 N 行（大文件试跑） | `--max-lines 100000` |
| `--selftest` | 内置自检，exit 0 即通过 | `--selftest` |

### 4.2 支持的格式标识

| `--format` 值 | 对应日志 |
|---|---|
| `auto` | 自动识别（默认，按首 200 行投票） |
| `logback` | Java Logback / Log4j 常见格式 |
| `pylog` | Python logging 默认格式 |
| `nginx_access` | Nginx combined access log |
| `nginx_error` | Nginx error log |
| `json` | 每行一个 JSON 对象 |
| `syslog` | syslog / systemd journal |
| `generic` | 通用兜底（抓时间戳 + 级别关键词） |

### 4.3 典型调用

```bash
# 1. 最省事：丢进去就跑
python scripts/log_scan.py --input app.log

# 2. 救火：只看故障那 10 分钟
python scripts/log_scan.py --input app.log \
  --since "2026-09-09 07:28" --until "2026-09-09 07:40" --bucket minute

# 3. 出正式巡检报告
python scripts/log_scan.py --input app.log --min-level WARN \
  --top 30 --report "巡检报告_20260909.md"

# 4. 发版后看新增错误
python scripts/log_scan.py --input after.log --baseline before.log --diff-only

# 5. Nginx 访问日志看状态码与慢请求
python scripts/log_scan.py --input access.log --format nginx_access --bucket hour

# 6. 多文件合并（滚动日志）
python scripts/log_scan.py --input app.log --input app.log.1 --input app.log.2

# 7. 接流水线
python scripts/log_scan.py --input app.log --json > scan.json
```

### 4.4 输出示例（节选）

```
=== 日志巡检报告 ===
文件: app.log        格式: logback（自动识别）
总行数: 184,203      解析成功: 183,911 (99.8%)
时间范围: 2026-09-09 00:00:03 ~ 2026-09-09 08:12:44

--- 级别分布 ---
  INFO   171,204  (93.1%)
  WARN     9,882  ( 5.4%)
  ERROR    2,801  ( 1.5%)
  FATAL       24  ( 0.0%)

--- Top 异常模板（已归并变量）---
  #1  1,842 次  [ERROR]  获取数据库连接超时 timeout=<NUM>ms pool=<STR>
      首次: 07:31:09    末次: 07:38:52    集中度: 99.4% 落在 8 分钟内
  #2    602 次  [ERROR]  订单 <NUM> 支付回调处理失败 traceId=<HEX>
      首次: 07:31:14    末次: 07:38:50    集中度: 98.1%
  #3     24 次  [FATAL]  线程池拒绝任务 queue=<NUM>/<NUM>
      首次: 07:33:02    末次: 07:37:41

--- 时间轴（分钟级，ERROR+）---
  07:28  |                      2
  07:29  |                      1
  07:30  |                      3
  07:31  |############          412    ← 突刺起点
  07:32  |####################  689
  07:33  |################      554
  ...

--- 伴生错误（突刺前 30 秒）---
  07:30:47  [WARN]  数据库主从延迟 <NUM>s 超过阈值
  07:30:52  [WARN]  慢查询 耗时 <NUM>ms SELECT ... FROM orders
  07:31:02  [ERROR] 获取数据库连接超时 ...

--- 故障特征匹配 ---
  命中: 连接池耗尽（置信度 高）
    依据: 「获取连接超时」+「线程池拒绝」+ 突刺前有慢查询与主从延迟
  命中: 慢查询导致连接堆积（置信度 中）

--- 根因假设（按可能性排序）---
  1. 慢查询导致连接占用时间变长 → 连接池耗尽 → 请求全面超时
     验证: 查 07:25~07:31 慢查询日志，看是否有新增全表扫描
  2. 主从延迟导致读请求重试放大
     验证: 查主从延迟指标曲线与重试次数
  3. 流量突增超出连接池容量
     验证: 查同时段 QPS，若无明显上升可排除

--- 处置建议 ---
  P0  定位并优化 07:25 后出现的慢查询（治本）
  P1  临时上调连接池上限 + 缩短获取超时，快速失败优于堆积
  P2  给「获取连接超时」加告警，阈值 50 次/分钟
  P3  慢查询日志接入日常巡检，不要等出事才看
```

### 4.5 退出码约定

| 退出码 | 含义 |
|---|---|
| 0 | 正常完成（无论有没有发现异常） |
| 2 | 参数错误 / 文件读不到 / 格式无法识别 |

---

## 五、错误模板归并：把日志读薄的核心手法

### 5.1 为什么必须归并

原始日志里这三行是三条记录：

```
订单 10086 支付回调处理失败 traceId=a1b2c3
订单 10087 支付回调处理失败 traceId=d4e5f6
订单 10088 支付回调处理失败 traceId=g7h8i9
```

对人来说它们是**同一个问题**。不归并的话，`grep ERROR | wc -l` 得到「2801 条错误」，你还是不知道有几种错。归并后是「3 类错误，其中一类占 66%」——这才是能行动的信息。

### 5.2 占位化规则（脚本内置）

| 原始内容 | 替换为 | 说明 |
|---|---|---|
| 连续数字 ≥2 位 | `<NUM>` | ID、耗时、端口、行号 |
| UUID | `<UUID>` | |
| 16 进制串 ≥6 位 | `<HEX>` | traceId、哈希 |
| IP 地址 | `<IP>` | |
| 时间戳片段 | `<TS>` | |
| 引号内内容 | `<STR>` | 变量文本 |
| 文件路径 | `<PATH>` | |
| 邮箱 / 手机号 | `<PII>` | 同时起脱敏作用 |
| URL 中的查询串 | `<QS>` | |

保留的是**骨架文本**，也就是程序里那句写死的日志文案。归并后同一句代码打出的日志自然聚成一条。

### 5.3 归并的三个坑

1. **占位太狠会把不同错误合并**。比如把所有单词都占位化，`连接超时` 和 `读取超时` 就分不开了。规则要只吃「明显是变量」的部分。
2. **占位太松等于没归并**。traceId 不占位，两万条各成一类。
3. **异常堆栈要单独处理**。Java 堆栈是多行，必须先做「多行合并成一条记录」再归并，否则每一行 `at com.xxx...` 都成独立记录。脚本已处理这一点（续行判定：以空白开头或以 `at ` / `Caused by` 开头）。

---

## 六、时间轴分析：四种分布形态对应四类根因

| 形态 | 图形特征 | 典型根因 | 首查方向 |
|---|---|---|---|
| **持续型** | 突然起来，一直高位，直到某刻断崖恢复 | 下游依赖挂了 / 配置错了 / 证书过期 | 依赖方日志、变更记录 |
| **脉冲型** | 周期性尖峰，之间恢复正常 | GC 停顿 / 定时任务 / 限流窗口 / 缓存集中过期 | GC 日志、cron 表、缓存 TTL |
| **阶梯型** | 缓慢单调上升，越来越多 | 资源泄漏（连接 / 内存 / 文件句柄）/ 磁盘渐满 | 资源指标曲线、`lsof`、`df` |
| **单点型** | 只在某一分钟出现一批，之后彻底消失 | 一次性网络抖动 / 单次发布重启 / 单机故障 | 发布记录、网络监控、主机事件 |

**关键动作：突刺起点往前推 5 分钟**。这个时间窗口里的 WARN 往往就是根因预兆——系统很少突然死，一般先有征兆（慢查询变多、延迟上升、重试增加）。

---

## 七、实战案例（三个）

### 案例一：接口大面积超时（连接池耗尽）

**现象：** 07:31 起接口超时率飙到 60%，08:05 自行恢复。

**扫描发现：**

- Top1 模板：`获取数据库连接超时 timeout=<NUM>ms`，1842 次，99.4% 集中在 8 分钟内 → **持续型**
- 伴生错误：突刺前 44 秒有「慢查询 耗时 <NUM>ms SELECT ... FROM orders」和「主从延迟超阈值」
- 故障特征命中：连接池耗尽（高）+ 慢查询导致连接堆积（中）

**推理链：** 慢查询出现 → 单个连接占用时间从 20ms 变成 3s → 池子被占满 → 后续请求排队等连接 → 全面超时 → 上游重试放大 → 雪崩。

**处置：** 治本是干掉慢查询（大概率是新加的没索引的查询）；止血是调小获取连接超时时间——**快速失败比堆积等待更能保住系统**，这一点很多人搞反了。

**验证：** 查 07:25~07:31 慢查询日志，找新出现的语句；对齐当天发布记录。

### 案例二：日志刷屏治理

**现象：** 每天 4000 万行日志，磁盘一周满一次，出事时根本没法看。

**扫描发现：** Top1 模板占全量 62%——是一条 `INFO 缓存未命中 key=<STR>`，正常业务行为被打成了日志。Top2 占 18%，是健康检查的访问日志。

**处置清单：**

| 项 | 动作 |
|---|---|
| 缓存未命中 | 降为 DEBUG，或改为计数指标而非逐条日志 |
| 健康检查访问 | Nginx 层排除该 path 不记 access log |
| 重复堆栈 | 同一异常连续出现时只打首次 + 计数 |
| 日志切割 | 按大小 + 时间双策略，保留 7 天 |

**效果口径：** 治理后日志量降到 12%，出事时 Top 异常一眼可见。**日志的价值在信噪比，不在体积。**

### 案例三：发版后新增错误检测

**做法：**

```bash
# 发版前一小时
python scripts/log_scan.py --input before.log --json > before.json
# 发版后一小时
python scripts/log_scan.py --input after.log --baseline before.log --diff-only
```

**输出只列新出现的模板**，这比看总错误数敏感得多——总数可能因为流量变化上下浮动，但「出现了一句以前从没出现过的错误」是明确信号。

**判读规则：**

- 新增模板 + 数量少（个位数）→ 边缘路径，记录待观察
- 新增模板 + 数量快速上升 → 立即评估回滚
- 老模板数量翻倍以上 → 可能是新版本放大了老问题
- 无新增模板 → 发版日志层面干净（但不代表功能没问题）

---

## 八、常见误判 Top 10（照着避坑）

| # | 误判 | 真相 | 正确姿势 |
|---|---|---|---|
| 1 | Top1 错误就是根因 | Top1 常常是雪崩后果，数量最多但最晚出现 | 按**首次出现时间**排序，不只按数量 |
| 2 | 错误数下降就是修好了 | 可能是服务彻底挂了没流量了 | 看错误率而非错误数，同时看请求总量 |
| 3 | 没有 ERROR 就没问题 | 超时、变慢、返回错数据都可能不打 ERROR | 结合耗时分布与业务指标 |
| 4 | 只看应用日志 | 磁盘、网络、内核层的问题在系统日志里 | 同时看 syslog / dmesg |
| 5 | 单机日志代表全局 | 可能只是一台机器有问题 | 多机采样对比 |
| 6 | 堆栈最上层就是问题点 | 常是框架层，业务代码在下面几层 | 找第一个自己包名的帧 |
| 7 | 时间对齐随便看 | 多机时钟不同步能差出几分钟 | 先确认 NTP，再对齐时间线 |
| 8 | 忽略 WARN | 突刺前的 WARN 往往是根因预兆 | 突刺前 5 分钟的 WARN 必看 |
| 9 | 相信日志里的时间就是事件时间 | 异步日志有缓冲延迟 | 高精度分析时注意这个偏差 |
| 10 | 一次扫描就下结论 | 日志只是证据之一 | 用指标与链路追踪交叉验证 |

---

## 九、日志规范建议（治本，减少下次排查成本）

1. **结构化优先**。JSON 行式日志比自由文本好解析十倍，字段固定：`ts / level / service / traceId / msg / 业务字段`。
2. **必带 traceId**。没有链路 ID，跨服务排查就是猜。
3. **日志文案写死不拼变量**。`"支付失败"` + 字段传参，而不是 `"订单" + id + "支付失败"`——前者天然可归并。
4. **级别用对**：业务失败（用户输错密码）是 INFO/WARN，不是 ERROR。ERROR 应该只留给「需要人介入」的情况。
5. **异常必带上下文**。只打 `NullPointerException` 没用，要带入参与关键状态。
6. **重复错误做收敛**。同一异常连续出现时打首次 + 每 N 次汇总一次。
7. **不打敏感信息**。手机号、身份证、密码、Token 一律脱敏或不打（这也是合规要求）。
8. **保留期与切割**。按大小 + 时间双策略，别等磁盘满。
9. **关键路径打耗时**。`cost=xxms` 是事后分析性能的唯一依据。
10. **上线前想清楚「出事时我要靠什么日志定位」**，缺什么现在补。

---

## 十、异常处理（脚本报错怎么办）

| 报错 | 原因 | 处理 |
|---|---|---|
| `无法识别日志格式` | 格式太特殊 | 用 `--format generic` 兜底，或给我几行样例我出解析规则 |
| `解析成功率过低（<50%）` | 格式选错或日志混合 | 用 `auto` 重试；混合日志先按来源拆文件 |
| `时间范围过滤后为空` | `--since/--until` 格式或时区不对 | 用 `YYYY-MM-DD HH:MM` 格式；确认日志是本地时间还是 UTC |
| `文件读取失败` | 路径含空格 / 权限 / 不存在 | 路径加双引号；确认可读 |
| 中文乱码 | 日志不是 UTF-8 | 先 `iconv` 转码；脚本固定按 UTF-8 读并跳过坏字节 |
| 内存吃紧 | 文件太大 | 加 `--max-lines` 试跑，或先按时间窗口切片 |
| 归并结果过于笼统 | 占位规则吃掉了有效信息 | 用 `--json` 看原始样例行，人工确认边界 |

**大文件处理姿势：** 先切片再分析，别硬吃。

```bash
# 按时间窗口切
awk '$0 >= "2026-09-09 07:00" && $0 <= "2026-09-09 08:00"' app.log > slice.log
# 或直接用脚本的窗口参数（内部流式读，不全量载入）
python scripts/log_scan.py --input app.log --since "2026-09-09 07:00" --until "2026-09-09 08:00"
```

---

## 十一、隐私与合规说明

1. **本地运行**：`log_scan.py` 不联网、不上传、不修改输入文件，分析结果只写你指定的报告路径。
2. **自动脱敏**：归并时手机号 / 邮箱会被占位为 `<PII>`，报告里不会原样出现。
3. **授权前提**：只分析你自有或已获授权的系统日志。分析他人系统日志需要明确授权，这是法律问题不是技术问题。
4. **凭证红线**：不从日志中提取密钥、Token、密码用于访问系统。如果日志里发现了明文凭证，正确动作是**立即轮换并修掉打印它的代码**，而不是拿去用。
5. **报告外发前脱敏**：巡检报告可能含内部服务名、表名、IP，对外分享前先过一遍。
6. **留存最小化**：分析用的日志副本用完即删，别在本地长期堆生产日志。

---

## 十二、和其他工具怎么配合

- **前置**：日志采集与集中（无论用什么方案），本 Skill 分析的是已经落到文件的日志。
- **并行**：指标监控看「有多严重」，链路追踪看「卡在哪一跳」，日志看「具体报了什么」。三者缺一都会误判。
- **后置**：巡检报告的结论进故障复盘文档与告警规则调整。
- **自动化**：`--json` 输出可被定时任务消费，错误率超阈值时触发通知。

```bash
# 每日巡检定时任务示例（伪代码逻辑）
python scripts/log_scan.py --input /var/log/app/app.log \
  --min-level WARN --report "/tmp/daily_$(date +%F).md" --json > /tmp/scan.json
# 然后由你的通知脚本读 scan.json，错误率超阈值就推送
```

---

## 十三、使用示例

- 这是昨天的应用日志，帮我做一次日常巡检，输出报告
- 服务在 07:31 到 07:40 之间大面积超时，这是日志，帮我定位根因
- 日志刷屏太厉害了，帮我找出占比最高的几类日志，哪些该降级
- 刚发版，对比一下发版前后的日志，只告诉我新出现的错误
- 这是 Nginx access log，帮我看下状态码分布和慢请求集中在哪些接口
- 从这份慢查询日志里找出最该优化的几条语句
- 我想每天自动跑一次日志巡检，给我脚本命令和定时任务的接法
- 这份日志的时间线帮我理成故障复盘能用的格式

---

## 十四、参考资料索引

| 文件 | 内容 |
|---|---|
| `references/01_主流日志格式速查.md` | 七种格式的字段结构、解析正则、坑点 |
| `references/02_日志级别与异常分级.md` | 级别用法规范、P0-P3 分级标准、误用纠正 |
| `references/03_错误模板归并方法.md` | 占位化规则、多行合并、归并边界与调参 |
| `references/04_常见故障特征库.md` | 20 类故障的日志指纹、伴生现象、验证方法 |
| `references/05_巡检报告与闭环.md` | 报告五段结构、时间轴四种形态、突刺算法选型、闭环验证与日志规范十条 |
| `scripts/log_scan.py` | 日志扫描器（解析 / 归并 / 时间轴 / 特征匹配 / 报告） |
| `hooks/guardrail.md` | 合规护栏与拒答边界 |

---

## 十五、一句话总结

日志排查的本质是**降噪 + 排序**：把二十万行归并成二十类，按首次出现时间排序，往突刺前推五分钟。这三个动作做完，根因基本就浮出来了。这个 Skill 把它们变成一条命令。


---

## GitHub 专属：关联资源

- 本系列更多技能仓库：[laoke-log-sentinel](https://github.com/muzhi-888/laoke-log-sentinel) · [laoke-regex-forge](https://github.com/muzhi-888/laoke-regex-forge)
- 局内人·老K 落地页与导航：https://muzhi-888.github.io/ju-nei-ren-lao-k/
